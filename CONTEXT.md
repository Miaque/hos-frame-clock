# 视频帧时间识别

为依赖方提供视频画面中日期时间的识别能力。

## Language

**视频帧**：由调用方从视频中提取的单帧图片，是本模块的识别对象。

**画面时间**：视频帧右上角叠加显示的完整日期与时间。缺少日期的时分秒不能单独确定画面时间，不自动补齐当天日期。
_Avoid_: 视频播放进度、文件创建时间

**识别区域**：视频帧中叠加画面时间的矩形区域，以帧宽高的比例描述，随摄像头布局而定。
_Avoid_: 裁剪区域、裁剪范围
